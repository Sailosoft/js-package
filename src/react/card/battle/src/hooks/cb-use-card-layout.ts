import { useMemo } from "react/jsx-runtime";
import { CBCardLayout } from "../interfaces/cb-card.interface.ts";

export function useCardLayoutCB() {
  const layout: Partial<CBCardLayout> = useMemo(() => {
    return {
      
    }
  }, [])
}